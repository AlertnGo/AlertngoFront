import React from "react";
import "./style.scss";
import MyProfile from "../../components/MyProfile";

const Me = (props) => {
  return (
    <main>
    <MyProfile />
    </main>
  );
};

export default Me;
